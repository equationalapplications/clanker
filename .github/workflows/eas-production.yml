name: Production Release and EAS Update

on:
  push:
    branches:
      - main
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  release:
    runs-on: ubuntu-latest

    # Security: Explicit permissions for semantic-release and EAS
    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      # Use GH_PAT (if available) for both checkout and semantic-release
      # This ensures consistent authentication and allows bypassing branch protection
      # Falls back to github.token if GH_PAT is not configured
      - name: 🏗 Setup repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GH_PAT || github.token }}
          persist-credentials: true

      - name: 🏗 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: 🏗 Setup Expo
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: npm ci

      - name: 📦 Install semantic-release extra plugins
        run: npm install --save-dev @semantic-release/changelog @semantic-release/git @semantic-release/npm

      - name: 📝 Bump Version and Release on Github
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT || github.token }}
        run: npx semantic-release

      # Uncomment to enable automatic EAS updates on production merges
      # - name: 🚀 Update Production with EAS
      #   run: eas update --auto --branch production --non-interactive
#      - name: Deploy to Firebase Hosting
#        run: npm run predeploy
#      - uses: FirebaseExtended/action-hosting-deploy@v0
#        with:
#          repoToken: '${{ secrets.GITHUB_TOKEN }}'
#          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_YOUR_BRIGHTLY_AI }}'
#          projectId: your-brightly-ai
#          channelId: live
