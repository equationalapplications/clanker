FROM node:22-slim
RUN apt-get update && apt-get install -y procps && rm -rf /var/lib/apt/lists/*
WORKDIR /app/cloud-agent
COPY cloud-agent/package.json cloud-agent/package-lock.json* ./
RUN npm install
COPY cloud-agent/tsconfig.json ./
CMD ["npm", "run", "dev"]
